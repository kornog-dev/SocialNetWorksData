{% extends "layout.html.twig" %}

{% block content %}
    <main id="dashboard" class="col-10 offset-2 p-5">
        <h1>Dashboard</h1>
        <section id="dashboard-networks" class="row">
            <h2 class="col-12">Réseaux Sociaux</h2>
            {% for network in networks %}
                <article class="col-2" data-network="{{ network.network.name|lower }}">
                    <h3><span class="{{ network.network.logoUrl }}"></span> {{ network.network.name }}</h3>
                    <p>
                        <span>{{ network.followerCount }}</span> followers
                    </p>
                    <p>
                        <span>{{ network.bestCover }}</span> meilleure couverture
                    </p>
                </article>
            {% endfor %}
        </section>
        <section id="dashboard-post-data" class="row">
            <h2 class="col-12">Couverture des publications 2022</h2>
            <section id="2022">
                <canvas id="dashboard-post-data-chart-2022" class="col-10" height="500"></canvas>
            </section>
        </section>
    </main>
    <script>
        datasets_url_2022 = "{{ path('dashboard_post_data_datasets', { 'year' : '2022' }) }}";
        datasets_url_2021 = "{{ path('dashboard_post_data_datasets', { 'year' : '2021' }) }}";
    </script>
{% endblock %}