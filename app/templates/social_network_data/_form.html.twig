<section class="mt-5">
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="col-4">
        <div class="mt-2">
            {{ form_label(form.followerCount, 'Nombre de followers', { 'attr' : { 'class' : 'form-label' }} ) }}
            {{ form_widget(form.followerCount, { 'attr' : { 'class' : 'form-control' }} ) }}
        </div>

        <div class="mt-2">
            {{ form_label(form.network, 'Réseau', { 'attr' : { 'class' : 'form-label' }} ) }}
            {{ form_widget(form.network, { 'attr' : { 'class' : 'form-control' }} ) }}
        </div>

        <button class="btn btn-light mt-4">{{ button_label|default('Enregistrer') }}</button>
    </div>

    {{ form_end(form) }}
</section>
