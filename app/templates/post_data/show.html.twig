{% extends 'base.html.twig' %}

{% block title %}PostData{% endblock %}

{% block body %}
    <h1>PostData</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_datum.id }}</td>
            </tr>
            <tr>
                <th>Likes</th>
                <td>{{ post_datum.likes }}</td>
            </tr>
            <tr>
                <th>Engagement</th>
                <td>{{ post_datum.engagement }}</td>
            </tr>
            <tr>
                <th>Cover</th>
                <td>{{ post_datum.cover }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_data_index') }}">back to list</a>

    <a href="{{ path('app_post_data_edit', {'id': post_datum.id}) }}">edit</a>

    {{ include('post_data/_delete_form.html.twig') }}
{% endblock %}
